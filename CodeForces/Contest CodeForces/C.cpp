#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;

    int vet[n-1];

    for(int i = 0; i < n; i++) {
        cin>>vet[i];
    }

    int i = 0, j = n-1;

    while (i <= j) {
        if(vet[i] < vet[j]) {

        }
    }
}
